# 第一阶段运行说明

第一阶段包括图片文本分割、GIM 匹配演示修复，以及 TRELLIS Gaussian 到 3DGS 数据目录的渲染。以下命令均从项目根目录执行。

## 一、SAGS 文本分割

该入口使用 SAGS 已有的 GroundingDINO 和 Segment Anything 权重，根据英文文本提示生成前景掩码。它输出的是主流程前端需要的二维分割结果，不执行交互式三维 Gaussian 投票。

已有权重：

- `third_party/SAGS/gaussiansplatting/dependencies/GroundingDINO/weights/groundingdino_swint_ogc.pth`
- `third_party/SAGS/gaussiansplatting/dependencies/sam_ckpt/sam_vit_h_4b8939.pth`

运行命令：

```bash
CUDA_VISIBLE_DEVICES=0 \
HF_HOME=/path/to/huggingface-cache \
HF_ENDPOINT=https://hf-mirror.com \
third_party/TRELLIS/.venv/bin/python tools/segment_image.py \
  --input path/to/input.png \
  --prompt "object name" \
  --output-dir path/to/segmentation_output
```

输出目录：

```text
segmentation_output/
├── mask.png          单通道前景掩码
├── cutout.png        带透明通道的前景图片
├── annotated.png     检测框预览
└── detections.json   检测框、文本和分数
```

成功标志：

```text
SAGS_SEGMENTATION_READY
```

## 二、GIM 官方演示

`third_party/gim/demo.py` 已为 `fast_make_matching_figure` 补充缺省参数，不改变模型加载、`model.match`、`model.sample` 和几何计算逻辑。

运行命令：

```bash
cd third_party/gim
CUDA_VISIBLE_DEVICES=0 .venv/bin/python demo.py --model gim_roma
```

输入固定为：

- `assets/demo/a1.png`
- `assets/demo/a2.png`

输出：

- `assets/demo/a1_a2_gim_roma_match.png`
- `assets/demo/a1_a2_gim_roma_warp.png`

成功标志：

```text
GIM_DEMO_READY
```

## 三、TRELLIS 到 3DGS 渲染

输入是 TRELLIS 导出的 Gaussian PLY。脚本复用 TRELLIS 已有的 Gaussian RGB、逆深度、绝对深度和相机渲染器。

运行命令：

```bash
CUDA_VISIBLE_DEVICES=0 \
third_party/TRELLIS/.venv/bin/python tools/render_trellis_3dgs.py \
  --input-ply path/to/trellis_gaussian.ply \
  --output-dir path/to/trellis_3dgs \
  --resolution 512 \
  --latitudes 10,20,30 \
  --views-per-latitude 30
```

输出目录：

```text
trellis_3dgs/
├── source/
│   ├── images/                 RGB 环绕渲染图片
│   ├── depths/invdepth/        逆深度 PNG
│   ├── depths/absdepth/        float32 绝对深度 RAW
│   └── sparse/0/
│       ├── cameras.txt         COLMAP 相机内参
│       ├── images.txt          COLMAP 相机外参
│       └── points3D.txt
└── model/
    ├── point_cloud/iteration_30000/point_cloud.ply
    ├── cameras.json
    └── cfg_args
```

成功标志：

```text
TRELLIS_3DGS_RENDER_READY
```

## 服务器 2 验收记录

验收日期：2026 年 8 月 15 日。

| 任务 | 输入 | 结果 |
| --- | --- | --- |
| SAGS 文本分割 | TRELLIS mailbox 图片，提示词 `mailbox` | 检测 1 个目标，mask 含 267458 个前景像素 |
| GIM | `assets/demo/a1.png` 和 `a2.png` | 生成 5603314 字节匹配图和 5031750 字节校正图 |
| TRELLIS 渲染 | `reproduction_outputs/trellis_smoke.ply` | 生成 8 张 256×256 图片及完整相机、深度和 3DGS 模型目录 |

验收产物位于：

- `reproduction_outputs/phase1/sags_mailbox`
- `reproduction_outputs/phase1/trellis_3dgs`
- `third_party/gim/assets/demo/a1_a2_gim_roma_match.png`
- `third_party/gim/assets/demo/a1_a2_gim_roma_warp.png`

三个命令均正常结束并生成非空文件。此处只确认功能链路可运行，不评价分割、匹配或渲染效果。
