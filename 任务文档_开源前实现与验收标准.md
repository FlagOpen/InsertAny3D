# InsertAny3D 后续任务与验收标准

## 文档目的

本文档用于记录项目面向公开开源前的剩余开发任务。当前目标是：在指定服务器、指定虚拟环境、权重和输入文件均准备正确的情况下，各模块能够按文档启动、运行并生成下一阶段所需的输出文件。

本阶段不要求评价模型效果、生成质量、指标优劣或异常输入处理，也不要求为使用者增加额外的防御性代码。只验收正确执行路径。

## 范围调整

`MVInpainter` 视为对比实验模型，不属于 InsertAny3D 的实际主流程。本任务不要求把 MVInpainter 接入主流程，也不把 MVInpainter 的推理结果列为发布前验收项。其代码和数据可以保留，主流程不为它创建或维护虚拟环境。

## 当前已具备的基础

- Hunyuan3D-2、TRELLIS、TRELLIS-old、GIM 的环境和主要扩展已经可以导入。
- Hunyuan3D-2 已完成真实推理并导出 GLB。
- TRELLIS 已完成真实推理并导出高斯 PLY。
- GIM 已完成真实匹配和稀疏点采样。
- SAGS 的环境、SAM、GroundingDINO 权重和 CUDA 扩展已经准备好，但尚未纳入一条完整验收命令。

---

# 第一阶段：模型与几何处理链路

第一阶段包括原任务中的第 2、3、4 项：SAGS、GIM 演示修复、TRELLIS 输出到 3DGS 渲染。

状态：已于 2026 年 8 月 15 日完成服务器 2 功能验收。运行命令、输出目录和验收记录见 `第一阶段运行说明.md`。

## 任务一：SAGS / LangSAM 分割入口

### 要求

1. 确定项目实际使用的 SAGS 或 LangSAM 推理入口，不要求重新设计模型代码。
2. 写清楚 Python 环境、权重路径、输入图片或输入场景路径和输出路径。
3. 在服务器 2 的现有环境中执行一次正确路径。
4. 输出至少包含后续流程可以读取的前景掩码或分割结果文件。
5. 若仓库原始脚本存在路径假设，只需在文档中规定正确的工作目录和命令；不要求增加通用路径探测。

### 验收标准

- 使用文档中的命令，进程正常结束，返回码为 0。
- 不出现缺少 Python 包、权重、CUDA 扩展或模型文件的错误。
- 指定输出文件实际生成，文件大小大于 0，格式可以被下一步骤读取。
- 只验收“能运行并产出文件”，不验收分割准确率。

## 任务二：修复 GIM 官方演示入口

### 要求

1. 修复 `third_party/gim/demo.py` 中 `fast_make_matching_figure` 的参数调用错误。
2. 保留现有模型加载、匹配和采样逻辑，不修改模型权重或推理算法。
3. 规定一个固定的正确工作目录和运行命令。
4. 运行后至少生成匹配可视化结果，或生成项目后续步骤需要的匹配数据文件。

### 验收标准

- 官方演示命令返回码为 0，不再出现缺少 `override_color0` 或 `override_color1` 的错误。
- `model.match` 和 `model.sample` 均被实际执行。
- 至少生成一个非空的匹配结果文件或可视化文件。
- 不要求检查匹配质量，也不要求增加异常输入处理。

## 任务三：TRELLIS 输出到 3DGS 渲染

### 要求

1. 明确 TRELLIS 输出对象到 3DGS 渲染器输入格式的转换步骤。
2. 使用现有 TRELLIS、3DGS Renderer 或相关扩展，不重新实现渲染器。
3. 规定输入资产、相机参数、输出目录和执行命令。
4. 输出后续 GIM/SAGS 可以使用的渲染图片和必要的相机或稀疏重建文件。

### 验收标准

- 从一个已经生成的 TRELLIS 资产开始，命令返回码为 0。
- 渲染输出目录被创建，并包含至少一张非空图片。
- 相机或位姿文件按约定格式生成，后续脚本能够读取。
- 不验收渲染视角是否最佳、不验收图像质量。

## 第一阶段交付物

- SAGS/LangSAM 的实际运行命令和输出说明。
- 修复后的 GIM 演示入口。
- TRELLIS 到 3DGS 的转换和渲染脚本或明确命令。
- 一份第一阶段运行记录，包含命令、返回码和输出文件路径。

---

# 第二阶段：通信与主流程串联

第二阶段包括原任务中的第 5、6 项：Python/Unity IPC 和主流程入口。

## 任务四：Python 与 Unity IPC

### 要求

1. 确认 Python 服务端实际入口文件；若 README 中的路径不存在，必须修正文档或补齐入口。
2. 明确服务端、端口转发、Unity 客户端三者的启动顺序。
3. 明确请求和响应的数据格式，包括图片、深度、相机位姿、生成资产和插入位姿的路径或字段。
4. 在服务器和 Unity 所在机器之间完成一次正确请求和响应。
5. 不要求处理断线、错误请求、并发和权限问题。

### 验收标准

- 按文档启动后，Python 服务端和 Unity 客户端均正常运行。
- Unity 能发送一个规定格式的请求，Python 能收到并返回响应。
- 至少有一项真实文件路径或任务参数完成往返传递。
- 端口、工作目录和启动命令在文档中唯一且明确。

## 任务五：InsertAny3D 主流程入口

### 要求

1. 提供一个主入口，按固定顺序调用已验收的 SAGS、TRELLIS、GIM、3DGS 渲染和 IPC 步骤。
2. 输入格式、目录结构和输出格式固定即可，不要求支持多种输入。
3. 主入口可以是 Python 脚本或一组严格顺序执行的脚本，但必须有单一的启动说明。
4. 输出至少包括生成的 3D 资产、渲染结果、匹配数据或位姿，以及传给 Unity 的结果路径。
5. 不要求加入参数校验、自动下载、自动选择 GPU 或失败重试。

### 验收标准

- 在指定服务器和指定输入下，从单一入口启动后，各步骤按顺序执行并返回码为 0。
- 每一步的输出文件都存在且非空，下一步骤使用的是上一步实际生成的文件。
- 最终输出能够被 IPC 发送给 Unity，或在没有 Unity 时保存在规定的输出目录。
- 日志中能够看出各阶段已执行完成。
- 不验收最终插入效果，只验收流程没有中断且产物链路完整。

## 第二阶段交付物

- Python/Unity IPC 启动和协议说明。
- 单一主流程入口及运行命令。
- 一份从输入到最终输出的完整运行记录。
- 更新后的 README，删除失效路径，补充环境、权重和目录要求。

---

# 发布前的非功能性检查

这些检查不属于上述运行阶段，但完成公开开源前必须处理：

1. 核对 TRELLIS 两个 `flexicubes` 子模块的 dirty 修改，以及 SAGS 发布补丁。
2. 补充所有第三方项目的许可证、版权和模型使用限制说明。
3. 明确哪些文件不随仓库发布，以及权重和大数据的下载方式。
4. 确保 README 中的命令、目录和实际文件一致。

## 不纳入本次验收

- MVInpainter 的主流程接入。
- 模型生成质量和视觉效果。
- 指标是否达到某个数值。
- 异常输入、断线、并发、自动恢复和用户友好提示。
